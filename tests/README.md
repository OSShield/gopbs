# Tests
These tests generate a bunch of data and create archives in various ways to compare the resulting archives.

## Preparation
Run the script "create-data.sh" to create a dataset in the .test-source directory. You can supply arguments to the script to control the size of the dataset.